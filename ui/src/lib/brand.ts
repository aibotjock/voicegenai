/**
 * Product branding, centralized.
 *
 * VoiceGenAI is the user-facing product name. The backend sidecar keeps its
 * stable internal name ("Presence Studio"); we surface that name only where
 * it aids debugging (diagnostics, logs).
 */
export const PRODUCT_NAME = "VoiceGenAI";
export const BACKEND_NAME = "Presence Studio";
